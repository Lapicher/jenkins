pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                // 
                echo "Build"
            }
        }
        stage('Test') { 
            steps {
                // 
                echo "TEST"
                echo "parametros del pipeline de jenkins:"
                echo params.NAME
            }
        }
        stage('Deploy') { 
            steps {
                // 
                echo "Deploy"
            }
        }
    }
}
